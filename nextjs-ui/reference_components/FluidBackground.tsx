"use client";

import { cn } from "@/lib/utils/cn";

/**
 * FluidBackground (Reverted)
 * 
 * The user requested to remove the background changes.
 * This component now renders nothing, allowing the default
 * CSS background (defined in globals.css) to be visible.
 */
export function FluidBackground({ className }: { className?: string }) {
    return null;
}
