/**
 * Error Boundary Components
 *
 * Exports for error handling components following hierarchical approach:
 * - ErrorBoundary: Page-level error boundary (full-page fallback)
 * - SectionErrorBoundary: Section-level error boundary (inline fallback)
 *
 * Reference: Story 35 AC-2, AC-4, AC-8 (Error Handling)
 */

export { ErrorBoundary } from "./ErrorBoundary";
export { SectionErrorBoundary } from "./SectionErrorBoundary";
